NETWORK=`current_network`
if [ "$NETWORK" = "bitterzoet"  ]; then
  scp ~/Downloads/*.torrent athena:~/downloads/watch && rm ~/Downloads/*.torrent
else
  scp ~/Downloads/*.torrent athenaext:~/downloads/watch && rm ~/Downloads/*.torrent
fi
